---
name: video-talkcraft
description: 为口播、文章和课程生成独立的视频素材与 B-roll。根据文案拆分视觉任务，设计每条独立背景和前景动作，使用 Codex 内置 image_gen 生成或编辑图片，并输出可交给剪辑软件使用的素材包。只负责素材规划与生成，不负责配音、字级时间戳、Remotion 成片、模板库或画廊。
---

# video-talkcraft 素材生成

这个 skill 的唯一职责是把一段文案变成一组清楚、可剪辑的独立视觉素材。每条素材都有自己的背景、主标题、二级标题和一个完整的视觉动作；素材之间保持统一的材质和字体，但不能只换文案复用同一个画面。

## 工作流程

1. 读取文案，按语义拆成素材清单。为每条写清楚：口播对应句、单一结论、表现类型（图文流程 / 动作演示 / 实物 B-roll / 真实截图）、主标题、二级标题和动作结果。
2. 先列完整的背景清单，保证每条有不同主体、场景或机位。背景上方约 40% 留安静深色空间，不放文字、品牌标志或伪造截图。
3. 使用 Codex 内置 `image_gen` 生成或编辑背景。编辑前先查看参考图；有本地参考图时传入全部参考图。保存原图、提示词和素材映射。
4. 规划前景动作。每条至少有“触发 → 变化/冲突 → 结果”三个状态；动作必须有接触、受力、约束或完成反馈。图文流程用节点和路径讲关系，动作演示用可辨识物体表达机制。
5. 生成交付包：背景原图、素材预览、文案对应表、逐条提示词、来源记录和可直接导入剪辑软件的文件。无配音时按相对时间设计节拍，不伪造声画对齐或时间戳。

## 固定视觉标准

- 默认竖屏 1080×1920、30fps；默认每条 8 秒、整批 20 条，用户指定的数量、时长和画幅优先。
- 默认风格为炭黑/黑曜石底、拉丝银金属、烟色玻璃、单一朱红强调色和定向电影光。用户指定风格时以用户指定为准。
- 每条顶部固定两行：主标题一行、二级标题一行。主标题至少 96px，二级标题至少 72px，使用本目录 `assets/fonts/puhui-{regular,bold}.ttf`，不依赖系统字体。
- 删除中间滚动说明、轮播正文和阶段标题。必要声明只放底部居中，默认不显示“AI 生成示意”。短标签贴近对应对象，不堆长句。
- 背景只做固定锚点的单向缓慢推进；不要往返漂移、抖动或整屏晃动。动态感来自前景文字、路径、对象动作、标注和克制扫光。
- 首帧必须已经可用，末尾保留最终构图后直接结束，不套片头片尾转场。每条素材都要在 390px 宽手机尺寸复看，确保两行标题、标签和主体不重叠或截断。
- 真实网页或产品画面使用实际截图，不用代码 mock 冒充真实证据。生成背景属于示意，不能冒充官方现场。

## 交付检查

检查每条是否有独立背景、两行顶部标题、清楚的动作因果、完整结果反馈、正确字体、足够字号、可读的首帧和尾帧，以及无重复模板。逐条记录素材文件、提示词、文案对应关系和事实属性（证据 / 示意 / 未来设想）。

详细的独立素材规则见 [references/cger-broll-standard.md](references/cger-broll-standard.md)。
