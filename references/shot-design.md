# 镜头分层设计：背景 / 主体 / 文字（实操工作单）

`cinematography.md` 的七层模型是完整词汇表；实际给每个镜头做设计时，先用**三面分层**这个
粗颗粒工作单把每面的内容和动效定下来，再展开成 SHOTBOOK 层矩阵。三面与七层的对应：

| 面 | 含七层中的 | 回答的问题 |
|---|---|---|
| 背景面 | L1 相机 + L5 视差背景 + L6 环境 | 这个镜头的"空间"是什么，它怎么一直活着 |
| 主体面 | L3 主体 + L4 附着 + L5 配角 + L7 遮罩 | 这一拍的唯一主角是谁，谁在配合它 |
| 文字面 | 字幕 + 花字/标注/标签（屏幕空间层） | 听觉信息的视觉对位物在哪 |

**每个镜头三面都必须有分配**（背景面永不缺席——这正是 PPT 感病根 1 的解法）；
写不出某面的动效就写"继承全局系统"（相机漂/idle/环境呼吸），但不许写"无"。

## 0. 40+ 默认构图纪律（2026-09-02 用户定版）

- 1 秒内能读懂主标题；一屏只讲 1 个视觉结论，主体组仍 ≤3，但默认争取 1–2 组。
- 文字全部映射到 design-language §2 的 1.5× 字阶；排不下就减字、拆卡、换镜，不得缩字或用 `scale()` 视觉缩小。
- “炫酷”优先选大数字、真实证据推近、地图/时间线、机械锁定、聚光和纵深；不用小字 HUD、密集 chip、
  紫蓝霓虹 glitch、快速抖屏或装饰性英文缩写。
- 深海军蓝/墨黑舞台与暖白证据卡交替，唯一强调色选正红或暖金之一；高对比优先于氛围感。
- 重要构图落定后至少 hold 1.2s；任何强运动后都给观众一个明确“读”的窗口。
- 每个导出 composition 另填 `HEAD/TAIL`：Frame 0 已有完整主视觉；末 12–18 帧最终构图 hold；两端 `transition: none`。

## 1. 各面的动效菜单

### 背景面（选 1 个空间基底 + 相机乐句，环境层自动继承）
- **空间基底**：视差网格（GridField, depth 0.5）/ 纯色幕底（design-language 的 canvas 档）/
  泛黄纸纹/档案底（CSS 渐变+噪声）/ 3D 星野或场景（three 桥）/ 全出血素材（Ken Burns 必开）
- **相机乐句**（shots.ts 一条连续曲线，按内容选型）：
  慢推 1.00→1.06（默认呼吸）/ 慢拉（沉降、冷却）/ 横移跟随（列举、传送带）/
  轨道环绕（3D 检视）/ dutch 保持（不安、僵局）/ 纵向爬行（读代码、读长图）/
  世界画布长镜头（longtake.tsx，见 cinematography.md §3.5）
- **环境响应**：分幕色温档位（ACTS 表）；本镜头有没有曝光脉冲点、vignette 收紧点

### 主体面（一拍一主角，接力线写清楚；以下入退场只作用于镜内元素和内部边界）
- **入场**（0.2–0.8s，比出场用力）：中心 slam（scale 3→1 / 1.8→1 backOut）/ 侧压入让位式 /
  虚→实 rack focus / 描边 draw-on（L7）/ 弹入堆叠（media-pop-in）/ 逐球逐条 stagger
- **hold**（必须 idle 微动，Live 包裹）：呼吸 + 附着层活动（辉光脉动、粒子漂、代码上滚、指针走动）
- **配角互动**（L5，每个动作答"配合谁"）：反向运动（环扩张 vs 卡收缩）/ 光束桥接注意力 /
  时间轴/佐证物侧入 / 集体退焦只留主体（Defocus）
- **让位**（新主体锚点 −6f 起，三选按"还回不回来"）：**真退场**（0.15–0.5s 出场后从版面消失，
  本拍后不再回指的元素优先选这个）/ 降权留守（缩 0.92 + 上移 + 暗 −66% + blur 3.2px，Live retireAt——
  留守元素**计入同屏预算**，浅底同屏留守 ≤1 组）/ 划线击落被顶替 / 碎成粒子（转场用）

### 文字面（字幕之外，屏幕空间不进相机）
- **字幕**：按画幅选型（design-language.md §5），全片样式唯一；**底部跟读字幕素排**——
  不加任何动效，唯一例外 keyword-pop-highlight 且全片 ≤3 次（用户明确要求才放宽）
- **强调字**（作用于标题/金句/独立文字层，不是底部字幕）：关键词 accent 换色 pop /
  花字三重描边逐字揭示（FlowerWord）/ 砸字（SmashWord）/
  字体对比重音（type-contrast-emphasis：换衬线斜体放大）——一句话最多一个强调
- **标注**：荧光笔扫词（素材上）/ 手绘圈注箭头 / callout 引出线 / 铅笔手绘（PencilDraw）
- **信息件**：人名条 / 章节编号 / 名词解释卡 / 数字滚动——入场走三段式，hold 带浮动

## 2. 镜头类型预设（七型，直接抄再改）

每型给出三面的默认分配；SHOTBOOK 里写明偏离预设的地方即可。

### ① 钩子镜头（片头 5–8s）
- 背景：Frame 0 直接给完整海报式舞台，不从纯色幕/遮罩/模糊里揭开；之后可做极缓纵深推进。
  结尾加速推只在后接**内部镜头**时使用；钩子本身单独导出时，末 12–18 帧保留主视觉，不推进式退场
- 主体：Frame 0 至少已有一个完整可读主视觉；Frame 1 后再做局部 punch、数字锁定或 1–2 任主体接力，
  不用 3 个高速小件连续 slam 轰炸 40+ 观众
- 文字：hero 字阶大字即主体；字幕从第 1 句就在

### ② 论证/叙事镜头（8–18s，最常见）
- 背景：网格或幕底 + 相机分 2–3 段乐句（沉降→跟随→推向证据）
- 主体：标题→徽章→素材卡的接力线；列举用传送带流（持续左漂）不用静态网格
- 文字：关键词 accent pop；点名实体必须有视觉对位物入场

### ③ 数据镜头
- 背景：幕底压暗一档，给图表让对比度；相机慢推向图表主柱/终值
- 主体：chart-grow 逐项生长（stagger=语音列举节奏），关键柱高亮 + punch；数字 NumberRoll ≤26 帧到终值
- 文字：结论行在数据落定后 0.3s 才出（先看到证据再看到判词）；单位 caption 阶后置

### ④ 证据/素材镜头（截图、实拍、聊天记录）
- 背景：幕底 + 相机极缓 Ken Burns（素材自己也带 1→1.04 慢推，两层速度错开）
- 主体：白边卡 + 唯一投影"摆上桌"（轻歪 1–2°）；荧光笔/红线划重点跟朗读同步；放大镜出细节
- 文字：标注文字勿叠素材文字（白底卡另起）；来源行 micro 阶常驻角落
- 标注几何换算（实战坑）：荧光笔/红框落点 = 原图坐标 × (卡内容宽/原图宽) **+ 卡内边距**——
  白边/padding 漏计会差 1–2 行高直接扫错行；先抽一帧量准再写坐标

### ⑤ 金句/转折镜头
- 背景：**整幕换底色**（浅↔深反差，章节感来源）+ 色温转档 + vignette 收紧；相机放慢或 dutch
- 主体：大字逐行浮现（quote-card），核心词最后落 + punch + 曝光脉冲；其余一切 Defocus
- 文字：这一型文字面即主体面；字幕可暂时隐去（大字本身就是台词）

### ⑥ 收尾镜头
- 背景：纵深/聚光运动在末 18 帧前完成；最终保护窗只保留低幅环境呼吸，不后拉、不压暗、不合拢 vignette
- 主体：符号回收与终句锁定提前完成；最后 12–18 帧停在完整 CTA/终句构图，不退场、不盖红/黑幕
- 文字：CTA 不低于 tagline，来源信息不低于 micro；末尾直接结束，不 fade、不 wipe、不配 transition SFX

### ⑦ B-roll 镜头（视频素材 + 人物素材同时在场）
- 背景：B-roll 满画幅出血即空间基底（自身运动已经在动，相机只叠极轻慢推，两层速度错开）
- 主体：**B-roll 是这一拍的主角，人物降级成角标常驻**——两条路二选一：
  圆形头像章（`host-shrink-to-chip◆`）或分割抠人贴角（保留真实人形）。
  **两路都必须落在左下或右下角**（chip 圆心在画面下 1/3 带内，2026-08-27 定版）。
  选型、方位与硬约束见 `host-footage.md`「人物与 B-roll 同屏」。
  三条不许：不许把人切走（口播变配音 PPT）、不许人物占满画幅压住 B-roll、不许角标里放静态头像。
- 文字：标注只落在 B-roll 的留白区；角标与字幕不同侧
- 人物出镜节奏（2026-09-02 收口）：片头 Hook 在 **Frame 0 直接以满幅/半身建立人物**（首句～8s），再缩位让台——
  0 帧就是角标会弱化人的连接感；之后人物形态切换按 cinematography.md §4.5 的布局事件预算走
  （每分钟 2–4 次、语义触发），换位遵守**错峰三段序**（旧面板全退 → 人物换位 → 新面板才进）
- 人脸安全区：人物在场的镜头开工先跑 `scripts/face_bbox.py` 拿实测 bbox，
  文字/卡片/字幕**及其背景**全时刻禁入；主信息面板放人物对侧



## 3. 写进 SHOTBOOK 的方法

1. 先给每镜头填三面工作单（三行表：背景面/主体面/文字面 → 内容 + 动效 + 接力线）；
2. 再展开成层矩阵（逐节拍、逐层、答"配合谁"），格式见 cinematography.md §4；
3. 自检：背景面是否全程活着？主体接力线有没有断（某拍无主角/双主角）？文字面强调是否超发（一句>1 个）？
   排版预算过了没（同屏主体组 ≤3 含降权留守、空象限在、hero 造型唯一、人脸安全区无侵入——
   全表 cinematography.md §4.5）？

范例：`shotbook-example.md` 的 S3 就是"③数据镜头"预设 + 偏离项（3D 基底、dutch 僵局感）的写法。
